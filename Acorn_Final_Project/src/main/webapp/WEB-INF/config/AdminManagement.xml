<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AdminManagement">

	<select id="foodInfoList" resultType="FoodInfoDTO">
		select fcode, ftitle, fprice, fimage, fcategory, content, fmainimage, foption, optionname, optionprice, stock
		from food
		natural join foodoption
		order by fcode asc, foption asc
	</select>
	
</mapper>